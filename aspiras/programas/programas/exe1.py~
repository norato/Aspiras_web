class Numero(int):

    def teste(self):
        lista = []
        for i in range(self):
            b = range(self)
            lista.append(b)
        for i in range(self):
            for j in range(self):
                lista[i].remove(j)
        for i in range(1, self + 1):
                lista[i-1].append(str(i) * i)
        return lista

